spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false

rental-service:
  url: http://localhost

seller-service:
  url: http://localhost

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
