spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
    discovery:
      enabled: false

product-service:
  url: http://localhost

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
