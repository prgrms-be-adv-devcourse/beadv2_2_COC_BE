spring:
  application:
    name: modi-gateway
  config:
    import: configserver:http://modi-config:8888

  cloud:
    config:
      discovery:
        enabled: true
        service-id: modi-config

#server:
#  netty:
#    access-log-enabled: true
#
#logging:
#  level:
#    reactor.netty.http.server.AccessLog: INFO

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://modi-discovery:8761/eureka/}