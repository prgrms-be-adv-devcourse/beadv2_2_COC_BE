package com.coc.modi.seller.settlement.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface SettlementBatchExecutionLogRepository extends JpaRepository<SettlementBatchExecutionLog, Long> {
}
